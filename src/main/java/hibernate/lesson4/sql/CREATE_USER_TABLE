CREATE TABLE USERS(
  USER_ID NUMBER,
  CONSTRAINT USER_PK PRIMARY KEY (USER_ID),
  USER_NAME NVARCHAR2(50) NOT NULL,
  PASSWORD NVARCHAR2(50) NOT NULL,
  COUNTRY NVARCHAR2(50),
  USER_TYPE NUMBER(1),
  CHECK (USER_TYPE BETWEEN 0 AND 1)
);

CREATE SEQUENCE USER_SEQUENCE
  MINVALUE 1
  START WITH 1
  INCREMENT BY 1
  CACHE 20;